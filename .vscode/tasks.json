{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker run",
            "type": "shell",
            "command": "docker run --rm -p 8080:80 -v \"${HOST_WORKSPACE}:/usr/share/nginx/html:ro\" nginx:latest",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
